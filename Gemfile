# frozen_string_literal: true

source "https://rubygems.org"

# Use GitHub Pages-compatible setup
gem "github-pages", group: :jekyll_plugins

# Required for remote themes
gem "jekyll-remote-theme"

# Optional plugins
group :jekyll_plugins do
  gem "jekyll-feed"
end

# Fix for Ruby 3+
gem "webrick"